apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: attendance-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/l-and-d/ld
    targetRevision: main
    path: helm/services/ld-attendance-service
    helm:
      releaseName: attendance-service
      valueFiles:
      - values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true